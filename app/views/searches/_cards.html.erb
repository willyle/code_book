<% if session[:user_id] %>
	<% @search.each do |search| %>
		<div class="recent-card">
			<% @tags = search.tags.all %>
			<% @links = search.links.all %>
			<div class="question"><%= search.title %>
				<p><%= search.note %></p>
			</div>
			<div class="answer-label">Answers
			  <img class="down" src="/assets/down.png">
			</div>
			<%= link_to card_delete_path(search: search), method: :delete, data: {confirm: "Are you sure?"}, remote: true do %>
				<div class="delete-card"><i class="fa fa-times"></i></div>
			<% end %>
			<% @links.each do |link| %>
				<% @resources = link.resources.all%>
				<div class="answer-dropdown">
			 		<div class="answer-text">Answer: wwww.stackoverflow.com
						"<%= link.title %>"
						<%= link.url %>
						<p>
							<% @resources.each do |resource| %>
								<%= resource.note %>
								<%= link_to answer_delete_path(resource: resource), method: :delete, remote: true do %>
									<div class="delete-answer"><i class="fa fa-times"></i></div>
								<% end %>
							<% end %>
						</p>
					</div>
					<div class="answer-language">
						<% @tags.each do |tag| %>
						<%= tag.language.name %>
						<% end %>
					</div>
				</div>
			<% end %>
		</div>
	<% end %>
<% end %>